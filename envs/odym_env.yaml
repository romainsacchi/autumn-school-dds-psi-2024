# in your terminal, run conda: conda env create -f bw25_env_win64.yaml

name: odym
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - jupyterlab
  - matplotlib
  - seaborn
  - pip:
      - git+https://github.com/romainsacchi/ODYM.git