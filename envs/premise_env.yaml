# in your terminal, run conda: conda env create -f premise_env.yaml

name: premise
channels:
  - romainsacchi
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - premise
  - jupyterlab
  - matplotlib
  - seaborn
  - constructive_geometries==1.0.0
  - activity-browser
  - ab-plugin-scenariolink